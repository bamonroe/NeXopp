# TODO — active task list for xopp_android.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list and journal for this project — active work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed work unchecked."
remove_dropped_items = "Remove dropped items with a one-line why."
finish_work = "When a task is fully finished (built, tested, documented), run `todo.py done <id>` to move it to FINISHED.toml."
documentation_is_part_of_the_task = "Every task that writes code also writes its documentation, in the same task and the same commit. Never add a separate follow-up task for documenting work that a task already did — a task whose docs are not updated is not finished, and no `done` until they are."
scope = "Only build features the .xopp format can represent on disk. If a capability cannot round-trip through the file, it is out of scope."

[[task]]
id = "shrink-palette-padding-around-icons"
title = "Shrink palette padding around icons"
description = "The radial palette has a lot of dead space around its icons. Reduce the padding/margins around each icon substantially so the overall palette shrinks noticeably."
status = "active"
level = "task"
category = "refactor"
urgency = "normal"
order = 10
created = 2026-08-05
tags = []
rebuild = true
emulator_debug = true

[[task]]
id = "match-palette-icons-to-toolbar-rail-icons"
title = "Match palette icons to toolbar rail icons"
description = "Replace the palette's icons with the same icon set used in the toolbar rail so the two are visually consistent."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 20
created = 2026-08-05
tags = []
rebuild = true
emulator_debug = true

[[task]]
id = "require-direct-hits-on-palette-icons-to-select"
title = "Require direct hits on palette icons to select"
description = "Selection currently uses nearest-match, so a tap far from an icon still selects it. Require the tap to land inside the icon's own bounds. More precision is fine — it makes dismissing the palette easier without accidental selection."
status = "active"
level = "task"
category = "bug"
urgency = "high"
order = 40
created = 2026-08-05
tags = []
rebuild = true
emulator_debug = true
