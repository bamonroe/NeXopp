# TODO — active task list for xopp_android.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list and journal for this project — active work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed work unchecked."
remove_dropped_items = "Remove dropped items with a one-line why."
finish_work = "When a task is fully finished (built, tested, documented), run `todo.py done <id>` to move it to FINISHED.toml."
scope = "Only build features the .xopp format can represent on disk. If a capability cannot round-trip through the file, it is out of scope."

[[task]]
id = "epic-radial-palette-tool-at-the-pen-tip"
title = "EPIC: radial palette tool at the pen tip"
description = "Large multi-part feature. A radial palette that pops up right at the pen tip location, made of two concentric rings: an inner ring of 8 segments and an outer ring of 16 segments. Any segment may be empty. Each filled segment holds a tool/action the user selects by flicking or tapping toward it. Sub-work to flesh out: (1) rendering the two-ring radial menu anchored at the pen tip, (2) hit-testing angle+radius to a segment, (3) invocation gesture (likely tied to the barrel button double-click or hold), (4) the palette configuration UI - how the user assigns tools to inner/outer slots and leaves others empty, (5) persistence of palette configuration, (6) possibly multiple palettes and how to switch between them. Break this epic into concrete child tasks before implementing."
status = "active"
level = "epic"
category = "feature"
urgency = "normal"
order = 20
created = 2026-08-04
tags = ["epic", "ui", "palette"]
rebuild = true
emulator_debug = true

[[task]]
id = "store-a-named-list-of-radial-palettes-with-an-active-one"
title = "Store a named list of radial palettes with an active one"
description = "Widen AppSettings/SettingsStore from a single radialPalette to an ordered list of named RadialPalettes plus an active index. Extend RadialPaletteCodec with a list encoding (record-separated palettes, forgiving decode as today) and migrate the existing single-palette pref into a one-entry list so no user loses their setup. Unit-test the codec round-trip and the migration."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 30
created = 2026-08-04
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "palette-manager-ui-add-rename-reorder-and-delete-palettes"
title = "Palette manager UI: add, rename, reorder and delete palettes"
description = "In PaletteSection/PaletteEditorLayout, add a manager row above the ring editor to create a palette, rename it, reorder it, delete it (never below one), and choose which palette the editor edits and which is active. Follow the presets rail pattern already used for tool presets. Document in README."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 40
created = 2026-08-04
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "add-a-switch-palette-slot-action-to-the-radial-palette"
title = "Add a switch-palette slot action to the radial palette"
description = "Add a SWITCH_PALETTE action to PaletteActionCatalog/PaletteActions carrying a target palette id, so a flick to that slot makes another palette active (and optionally reopens the ring in place at the pen tip). Cover the codec token and the hit-test dispatch with unit tests; document in README."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 50
created = 2026-08-04
tags = []
rebuild = true
emulator_debug = true

[[task]]
id = "add-a-palette-invocation-setting-for-styluses-without-a-barrel-button"
title = "Add a palette-invocation setting for styluses without a barrel button"
description = "Introduce a PaletteInvocation enum in InputSettings (BARREL_DOUBLE_CLICK default, PEN_TIP_LONG_PRESS, TWO_FINGER_TAP) with a SettingsStore key and a settings-screen choice, so invocation is user-selectable rather than hardwired to EditorRegions.onBarrelDoubleClick. Unit-test the setting round-trip; document in README."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 60
created = 2026-08-04
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "open-the-radial-palette-on-a-pen-tip-long-press"
title = "Open the radial palette on a pen-tip long-press"
description = "In DrawingSurfaceView, arm the existing longPressMs timer on a stylus-down when PaletteInvocation is PEN_TIP_LONG_PRESS, cancel it once the pointer moves past touch slop so it never steals a stroke, and open the two-ring palette at the down point. Verify on the emulator with a simulated stylus hold."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 70
created = 2026-08-04
tags = []
rebuild = true
emulator_debug = true

[[task]]
id = "open-the-radial-palette-on-a-two-finger-tap"
title = "Open the radial palette on a two-finger tap"
description = "Implement the TWO_FINGER_TAP invocation: a quick two-pointer finger tap that does not become a pan or pinch opens the palette centred between the two fingers. Must not steal the existing pan/zoom gestures or the hand double-tap; guard on movement and duration thresholds. Verify on the emulator."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 80
created = 2026-08-04
tags = []
rebuild = true
emulator_debug = true
