# TODO — active task list for xopp_android.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list and journal for this project — active work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed work unchecked."
remove_dropped_items = "Remove dropped items with a one-line why."
finish_work = "When a task is fully finished (built, tested, documented), run `todo.py done <id>` to move it to FINISHED.toml."
documentation_is_part_of_the_task = "Every task that writes code also writes its documentation, in the same task and the same commit. Never add a separate follow-up task for documenting work that a task already did — a task whose docs are not updated is not finished, and no `done` until they are."
scope = "Only build features the .xopp format can represent on disk. If a capability cannot round-trip through the file, it is out of scope."

[[task]]
id = "long-press-a-preset-slot-to-overwrite-it"
title = "Long-press a preset slot to overwrite it"
description = "Long-pressing an existing saved preset should overwrite it with the currently active tool settings, so presets can be cleaned up in place instead of only being appended."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 30
created = 2026-08-05
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "order-saved-presets-by-slot-number"
title = "Order saved presets by slot number"
description = "Presets are currently identified by name. Track an explicit order instead so the palette can always map preset 1, 2, 3, 4, 5... to fixed slots. Names stay useful but ordering is what the palette keys off."
status = "active"
level = "scope"
category = "refactor"
urgency = "normal"
order = 40
created = 2026-08-05
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "tint-the-preset-icon-with-the-preset-s-color"
title = "Tint the preset icon with the preset's color"
description = "In the palette, a preset slot shows a plain bookmark/star icon. Color that icon with the preset's active color so slots are identifiable at a glance."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 50
created = 2026-08-05
tags = []
rebuild = true
emulator_debug = false
