# TODO — active task list for xopp_android.
#
# Single source of truth for active work (see CLAUDE.md -> documentation map).
# Edit through the todo skill so ids, ordering, and metadata stay consistent:
#   scripts/todo.sh <command>      (see .claude/skills/todo/SKILL.md)
# Hand-editing works too — it is plain TOML — but the skill keeps it tidy.

schema_version = 1

[meta]
title = "TODO"
kind = "todo"
purpose = "The live task list and journal for this project — active work only."
documentation_map = "CLAUDE.md"
archive = "FINISHED.toml"

[meta.rules]
add_proposed_work = "Add proposed work unchecked."
remove_dropped_items = "Remove dropped items with a one-line why."
finish_work = "When a task is fully finished (built, tested, documented), run `todo.py done <id>` to move it to FINISHED.toml."
documentation_is_part_of_the_task = "Every task that writes code also writes its documentation, in the same task and the same commit. Never add a separate follow-up task for documenting work that a task already did — a task whose docs are not updated is not finished, and no `done` until they are."
scope = "Only build features the .xopp format can represent on disk. If a capability cannot round-trip through the file, it is out of scope."

[[task]]
id = "reject-absurd-png-dimensions-in-pngdecode-before-allocating"
title = "Reject absurd PNG dimensions in PngDecode before allocating"
description = """
PngDecode.readHeader accepts any positive width/height from IHDR, so a malformed or hostile .xopp can claim a size whose row buffer overflows Int arithmetic: Header.stride is (width * bitsPerPixel + 7) / 8, and unfilterRows/deinterlace then allocate height * stride bytes. A width near Int.MAX_VALUE makes that product overflow to a negative size (NegativeArraySizeException) or a huge one (OutOfMemoryError), either of which escapes decode() as a throw. That breaks the contract the .rnote writer leans on — decodeToRaw returns null for anything it cannot read, never throws, so the picture is skipped and reported instead of sinking the save.

In app/src/main/java/com/nexopp/format/rnote/PngDecode.kt, in readHeader, after the width/height read, return null when the image cannot fit: compute the full-size byte count as a Long ((width.toLong() * bitsPerPixel + 7) / 8 * height) and reject anything over a sane ceiling (say 256 MiB of samples), alongside the existing width <= 0 / height <= 0 check. Keep it in readHeader so both the interlaced and non-interlaced paths are covered by one gate.

Verify in RawImageCodecTest: hand-build a PNG whose IHDR claims 0x40000000 x 0x40000000 (the existing assemble() helper takes width/height directly) and assert RawImageCodec.decodePng returns null rather than throwing. Run scripts/build.sh testDebugUnitTest."""
status = "active"
level = "task"
category = "bug"
urgency = "low"
order = 40
created = 2026-08-15
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "jpeg-decode-baseline"
title = "Hand-roll a baseline JPEG decoder (JpegDecode.kt)"
description = "Create app/src/main/java/com/nexopp/format/rnote/JpegDecode.kt, a dependency-free baseline-sequential JPEG decoder in the exact style of PngDecode.kt: pure JVM (no android.graphics), object with a single decode(bytes: ByteArray): RawImage? entry point, returns null on anything malformed or unsupported, never throws. Support: SOI/APPn/COM skip, DQT (8- and 16-bit tables), DHT, SOF0 (baseline; 8-bit precision; 1 or 3 components), SOS, restart markers (DRI/RSTn), Huffman entropy decode, dequantise, 8x8 inverse DCT, 4:4:4 / 4:2:2 / 4:2:0 chroma upsampling, YCbCr-to-RGB (and greyscale passthrough), output as RawImage with opaque alpha. Return null for SOF2/progressive and any other SOFn, 12-bit precision, CMYK/4-component, or arithmetic coding. Add JpegDecodeTest.kt with fixture JPEGs under app/src/test/resources/fixtures/jpeg/ generated by ImageMagick (document the generation commands in a README there, like the png fixtures): at least greyscale, 4:4:4, 4:2:0, a restart-interval file, and a progressive file that must return null; compare decoded pixels against ImageMagick-dumped raw RGB within a small per-channel tolerance (IDCT rounding differs between decoders, allow <= 2). Verify with scripts/build.sh testDebugUnitTest. Do not touch RawImageCodec yet - that is the follow-up task."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 50
created = 2026-08-15
tags = []
rebuild = true
emulator_debug = false

[[task]]
id = "jpeg-wire-rawimagecodec"
title = "Wire JPEG decoding into RawImageCodec and flip the docs"
description = "Depends on jpeg-decode-baseline. In RawImageCodec.decodeToRaw (app/src/main/java/com/nexopp/format/rnote/RawImageCodec.kt), sniff the first bytes: PNG signature -> PngDecode as today; JPEG SOI (0xFF 0xD8) -> JpegDecode.decode; anything else or a null decode -> null as today, so RnoteSnapshotWriter and RnoteExportWarnings.isEncodable pick JPEG support up automatically with no changes of their own. Add a RawImageCodecTest case proving a fixture JPEG now encodes (and a progressive one still reports as unreadable via isEncodable). Update docs/architecture.md: flip the bitmap-pixels row of the Images feature-gap matrix from report-for-JPEG to keep (baseline; progressive still reports), and adjust the 2026-08-13 JPEG note to point at the 2026-08-15 decision. Update README.md if it mentions the JPEG limitation. Regenerate nothing; validate one exported .rnote containing a JPEG-backed image with rnote-cli test per docs/tools.md. Verify with scripts/build.sh testDebugUnitTest."
status = "active"
level = "task"
category = "feature"
urgency = "normal"
order = 60
created = 2026-08-15
tags = []
rebuild = true
emulator_debug = false
